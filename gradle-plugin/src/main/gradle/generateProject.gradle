apply plugin: 'ws.osiris.project'

defaultTasks 'generateProject'

ext {
    osirisVersion = '0.9.0-SNAPSHOT'
    kotlinVersion = '1.2.30'
}

buildscript {
    ext.osirisVersion = '0.9.0-SNAPSHOT'

    repositories {
        jcenter()
    }

    dependencies {
        classpath "ws.osiris:osiris-gradle-plugin:$osirisVersion"
    }
}
